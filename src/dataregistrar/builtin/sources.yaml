# Shipped sources. A user or project layer can override any entry by id, or disable it.
sources:
  - id: uci
    adapter: uci
    kinds: [dataset]
    cache_ttl: 86400
  - id: hf
    adapter: huggingface
    kinds: [dataset]
    cache_ttl: 3600
    config:
      limit: 20
